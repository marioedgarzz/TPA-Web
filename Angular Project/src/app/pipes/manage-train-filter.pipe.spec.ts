import { ManageTrainFilterPipe } from './manage-train-filter.pipe';

describe('ManageTrainFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ManageTrainFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
