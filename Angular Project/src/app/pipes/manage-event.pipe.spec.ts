import { ManageEventPipe } from './manage-event.pipe';

describe('ManageEventPipe', () => {
  it('create an instance', () => {
    const pipe = new ManageEventPipe();
    expect(pipe).toBeTruthy();
  });
});
