import { ManageHotelPipe } from './manage-hotel.pipe';

describe('ManageHotelPipe', () => {
  it('create an instance', () => {
    const pipe = new ManageHotelPipe();
    expect(pipe).toBeTruthy();
  });
});
